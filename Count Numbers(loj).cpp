#include<bits/stdc++.h>
using namespace std;

int main()
{
    int t;cin>>t;
    while(t--)
    {
        string x;
        getline(cin,x);
        int len=x.length();
        for(int i=0;i<len;i++)
        {
            if(x[i]!=' ')
            {
                if(' '!=x[i+1])
                count++
            }
        }
    }
    return 0;
}
