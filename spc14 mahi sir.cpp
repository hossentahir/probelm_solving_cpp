#include<bits/stdc++.h>
using namespace std;

int main()
{
    int n,m;cin>>n>>m;
    while(n--)
    {
         int num; cin>>num;
         while(num>0)
         {
             int last_digit= num%10;
             if(last_digit)
         }

    }
    return 0;
}
